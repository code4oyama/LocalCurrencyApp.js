{
  "name": "local-currency-app",
  "version": "1.0.0",
  "description": "地域通貨決済アプリ（Capacitor + Vanilla JS）",
  "private": true,
  "scripts": {
    "dev": "http-server web -p 5173 -c-1",
    "build": "echo '静的Webアプリのためビルド不要'",
    "cap:sync": "cap sync",
    "cap:copy": "cap copy",
    "cap:open:ios": "cap open ios",
    "cap:open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor-mlkit/barcode-scanning": "^8.0.0",
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "http-server": "^14.1.1"
  }
}
